import React from 'react';
import Routes from './routes';
import './global.css';

function App() {
  return (
    <Routes />
  );
}

export default App;










//Conceitos do React:
// Componente: função que retorna HTML
// JSX (JavaScript XML): quando HTML está integrado dentro do js
// Propriedades: atributos repassados para componentes do HTML
// Estado: informação mantida pelo componente
// Imutabilidade: não se pode alterar o estado de forma direta
